# Copy this to .env on the VPS (never commit .env)
# Used by compose.yaml for non-secret configuration.

# Comma-separated list of domains/IPs Django will accept requests for
ALLOWED_HOSTS=yourdomain.org,www.yourdomain.org
